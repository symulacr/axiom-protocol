# Frontend Fix Wave F2 — Phase 1 (Performance & Data Flow)

**Date:** 2026-07-06  
**Status:** ✅ Verified (typecheck + build)

---

## Agents

| Agent | Files | Issues |
|-------|-------|--------|
| F2-A | AgentDetail, useAgentEvents/Performance/Health | C-6, P1-1 |
| F2-B | useAgentEvents, utils/events | H-7, P1-2, H-12, P1-5 |
| F2-C | useOrchestratorTick | H-8, P1-4, H-13 |
| F2-D | ChatPage | H-5, P1-3 |
| F2-E | main.tsx | P1-6 |
| F2-F | MintForm, PaymentPanel | C-2-ext |

---

## Highlights

- **AgentDetail:** Hooks gated by `activeSection` — no activity/performance/health fetch on mount.
- **useAgentEvents:** WS events merged with HTTP; `eventTokenId` filter; refetch only on first WS connect.
- **useOrchestratorTick:** Single abort path; WS onclose/onerror + 60s connect timeout; cleanup on cancel/unmount.
- **ChatPage:** Stable `message.id` keys via `crypto.randomUUID()`.
- **main.tsx:** QueryClient `staleTime: 10s`, `refetchOnWindowFocus: false`, `retry: 1`.
- **MintForm/PaymentPanel:** Chain-aware contract addresses on all writes.

---

## Remaining (Phase 2+)

UI duplication (H-1–H-3), ChatPage monolith (H-4), a11y tabs (H-6), ChatPage chain writes + apiFetch (H-15, ChatPage C-2), wagmi reactive config (H-9), resetPay (H-10), ~63 medium/low items.