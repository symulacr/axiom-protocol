# Frontend Fix Wave F1 — Phase 0 (Critical)

**Date:** 2026-07-06  
**Orchestrator:** Fixing Orchestrator  
**Status:** ✅ Verified

---

## Agents & Assignments

| Agent | Files | Issues |
|-------|-------|--------|
| F1-A | `hooks/useEventHistory.ts` | C-1 / P0-1 |
| F1-B | `hooks/useDeposit.ts`, `useTransfer.ts`, `usePayment.ts` | C-2 / P0-2a–c |
| F1-C | `components/ExecutePanel.tsx` | C-3, C-4, H-14 / P0-3, P0-2d |
| F1-D | `pages/ChatPage.tsx` | C-5 / P0-4 |
| F1-E | `components/PaymentPanel.tsx` (line 86) | Pre-existing TS blocker |

---

## Before → After Highlights

### P0-1 Event accumulation (F1-A)
- **Before:** Each `?since=` poll replaced timeline with delta only.
- **After:** `mergedEventsRef` + dedupe `chainId:txHash:logIndex`, chronological sort, cap 500.

### P0-2 Chain-aware writes (F1-B, F1-C)
- **Before:** `getAxiom*Address()` without chainId → Galileo default.
- **After:** `useChainId()` passed to all write-path getters in deposit, transfer, payment, ExecutePanel tick.

### P0-3 ExecutePanel hooks (F1-C)
- **Before:** Conditional `useAgents()`; `useCallback` after early return.
- **After:** Unconditional hooks; connect guard is JSX-only after all hooks.

### P0-4 Chat throttle (F1-D)
- **Before:** `setStreamText` on every SSE token.
- **After:** 50ms throttle via ref + timer; flush on stream end/unmount.

---

## Verification

```bash
pnpm --filter @axiom/frontend typecheck  # ✅
pnpm --filter @axiom/frontend build      # ✅
```

---

## Remaining Critical

| ID | Issue | Next Wave |
|----|-------|-----------|
| C-6 | AgentDetail eager hooks | F2 |